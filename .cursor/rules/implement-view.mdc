---
description: implement a view using common interface and controllers
globs: 
alwaysApply: false
---
Bitte implementiere im common-ui package den platformunabhängigen View oder Widget interface den entsprechenden Controller richtig, passe notfalls auch das interface dazu an. 

Passe in der android version die View oder Widget implementierung so an, dass sie eine Instanz vom entsprechenden Controller nutzt.

Pase in der GNOME version die View oder Widget implementierung so an, dass sie eine Instanz vom entsprechenden Controller nutzt.