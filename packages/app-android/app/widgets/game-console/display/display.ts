import { StackLayout } from "@nativescript/core";

/**
 * Placeholder Display widget for the GameConsole.
 */
export class Display extends StackLayout {
  constructor() {
    super();
  }

  /**
   * Initialize the display (e.g., with memory access).
   * Placeholder for now.
   */
  public initialize(/* memory: Memory */): void {
    console.log("Display initialized (placeholder)");
  }
}
