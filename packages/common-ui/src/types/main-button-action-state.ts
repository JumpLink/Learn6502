/**
 * Represents the enabled state for different button actions
 */
export interface MainButtonActionState {
  assemble: boolean;
  run: boolean;
  resume: boolean;
  pause: boolean;
  reset: boolean;
  step: boolean;
}
