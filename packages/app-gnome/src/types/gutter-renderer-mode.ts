/**
 * Display modes for the GutterRendererAddress
 */
export enum GutterRendererMode {
  /** Display line numbers as normal numbers */
  NORMAL = 'normal',
  /** Display line numbers as hexadecimal addresses */
  HEX = 'hex'
}
