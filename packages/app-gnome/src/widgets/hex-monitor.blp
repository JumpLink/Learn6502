using Gtk 4.0;
using Adw 1;
using GtkSource 5;

template $HexMonitor: Adw.Bin {
  ScrolledWindow scrolledWindow {
    height-request: 200;
    vexpand: true;
    has-frame: false;
    $SourceView sourceView {
      editable: false;
      language: 'hex';
    }
  }
}
