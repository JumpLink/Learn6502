using Gtk 4.0;
using Adw 1;

template $HexMonitor: Adw.Bin {
  $SourceView sourceView {
    editable: false;
    language: 'hex';
    height-request: 200;
    vexpand: true;
    line-numbers: true;
  }
}
