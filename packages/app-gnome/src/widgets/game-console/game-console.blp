using Gtk 4.0;
using Adw 1;

template $GameConsole: Adw.Bin {
  name: "game-console";
  Box {
    halign: center;
    valign: center;
    orientation: vertical;

    $Display display {}

    $GamePad gamePad {
      margin-top: 30;
    }
  }
}
