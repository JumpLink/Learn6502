using Gtk 4.0;
using Adw 1;

template $Display: Adw.Bin {
  name: "display";
  halign: center;
  valign: start;

  Frame {
    DrawingArea drawingArea {
      content-width: bind template.displayWidth;
      content-height: bind template.displayHeight;
      focusable: true;
    }
  }
}
