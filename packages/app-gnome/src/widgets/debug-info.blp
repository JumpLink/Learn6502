using Gtk 4.0;
using Adw 1;

template $DebugInfo: Adw.Bin {
  ScrolledWindow scrolledWindow {
    height-request: 200;
    vexpand: true;
    has-frame: false;
    TextView textView {
      hexpand: true;
      hexpand-set: true;
    }
  }
}
