using Gtk 4.0;
using Adw 1;

template $MainButton  : Adw.Bin {
  name: "main-button";
  Gtk.Revealer revealer {
    transition-type: slide_up;
    reveal-child: true;
    child: Gtk.Button button {
      vexpand: true;
      hexpand: true;
      halign: center;
      styles [
        "suggested-action"
      ]
    };
  }
}
